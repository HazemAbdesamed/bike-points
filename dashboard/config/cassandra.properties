connector.name=cassandra

cassandra.contact-points=${ENV:CASSANDRA_HOST}
cassandra.load-policy.dc-aware.local-dc=datacenter1

cassandra.username=${ENV:CASSANDRA_USER}
cassandra.password=${ENV:CASSANDRA_PASSWORD}